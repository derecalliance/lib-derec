# derec-proto

Generated Rust protobuf types for the DeRec protocol.

This crate contains the Rust types generated from the DeRec `.proto` schema files.

Most application developers should use `derec-library` instead of depending on this crate directly.
