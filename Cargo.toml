[workspace]
resolver = "3"
members = [
    "cryptography",
    "library",
    "protobufs",
    "bindings/rust",
]

[patch.crates-io]
derec-cryptography = { path = "cryptography" }
derec-proto = { path = "protobufs" }
derec-library = { path = "library" }
